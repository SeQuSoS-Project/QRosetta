FROM nginx:alpine

# Remove default nginx config
RUN rm /etc/nginx/conf.d/default.conf

# Copy our minimal config
COPY nginx-frontend.conf /etc/nginx/conf.d/

# Copy static frontend files to nginx serving directory
COPY rosetta-api/static/ /usr/share/nginx/html

# Expose port 8080 for Google Cloud Run
EXPOSE 8080
