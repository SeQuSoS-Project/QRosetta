apiVersion: v1
kind: Secret
metadata:
  name: allas-s3-credentials
type: Opaque
stringData:
  S3_ACCESS_KEY: "${S3_ACCESS_KEY}"
  S3_SECRET_KEY: "${S3_SECRET_KEY}"
  S3_ENDPOINT_URL: "${S3_ENDPOINT_URL:-https://a3s.fi}"
  S3_REGION: "${S3_REGION:-default}"
  S3_BUCKET_NAME: "${S3_BUCKET_NAME}"