# Runners are no longer permanent Deployments.
# In kubernetes execution mode (EXECUTION_MODE=kubernetes), the rosetta-api
# spawns each runner as a short-lived BatchV1 Job via the K8s API.
# Runner images must be present in the registry (pushed via scripts/rahti_push.sh).
# This file is intentionally empty — apply it safely with: oc apply -f infra/rahti/runners.yaml
apiVersion: v1
kind: List
items: []
